{
  "scripts": {
    "prepare-client": "cd client && npm install && npm run build",
    "prepare-server": "cd server && npm install",
    "final-start": "npm run prepare-client && npm run prepare-server",
    "start-server": "cd server && npm run dev && cd ../../client/ && npm start"
  }
}
